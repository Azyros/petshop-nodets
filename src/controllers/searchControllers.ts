import {Request, Response} from 'express'

/*criando a rota, não vamos colocar o rerder inda pois não temos a parte visual da página*/
export const search = (req:Request, res: Response) =>{


    //res.render("pages/search")
}